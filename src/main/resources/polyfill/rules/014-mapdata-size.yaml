name: "MapData Size"
pattern: |
    1. Let $count:var be 0.
    1. For each element $elem:var of $base:ref, do
      1. If $elem:var.[[Key]] is not ~empty~, set $count:var to $count:var + 1.
replace: |
    1. Let $count:var be IN__MapDataSize($base:ref).
predicate: $base -> isMapData
