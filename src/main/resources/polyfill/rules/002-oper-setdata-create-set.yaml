name: "[OPER] SetData Create (Set)"
pattern: |
  1. Set {{ base : ref }} to a new empty List.
replace: |
  1. Set {{ base }} to IN__SetDataCreate().
where:
  - isSetData(base)
  - { isSetData: "_ (.[[SetData]] | .[[WeakSetData]]) .copy*" }
