def <AUX>:__GET_TYPED_ARRAY_INFO_FROM_TYPE__(
  type: Unknown, // TODO TypedArray element type
): Unknown = {
  let keys = (keys @TYPED_ARRAY)
  let idx = 0
  while (< idx (sizeof keys)) {
    let key = keys[idx]
    let obj = @TYPED_ARRAY[key]
    if (= obj.ElementType type) return obj
    idx = (+ idx 1)
  }
}
