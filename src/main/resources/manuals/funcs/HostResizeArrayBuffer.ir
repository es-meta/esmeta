def HostResizeArrayBuffer(
  buffer: Record[ArrayBuffer],
  newByteLength: Int[NonNeg],
): Normal[Enum[~handled~, ~unhandled~]] | Throw = {
  call %0 = clo<"NormalCompletion">(~unhandled~)
  return %0
}
