def <AUX>:__REMOVE_ELEM__(
  elem: Any,
  list: List,
): List = {
  let from = 0
  let to = 0
  while (< from list.length) {
    if (! (= list[from] elem)) {
      list[to] = list[from]
      to = (+ to 1)
    }
    from = (+ from 1)
  }
  while (< to list.length) { pop list > _ }
  return list
}
