def <NUM>:BigInt::exponentiate(
  base: BigInt,
  exponent: BigInt,
): Normal[BigInt] | Abrupt[throw] = {
  if (< exponent 0n) {
    call %0 = clo<__THROW__>("%RangeError.prototype%")
    return %0
  }
  if (&& (= base 0n) (= exponent 0n)) {
    return 1n
  }
  return (** base exponent)
}