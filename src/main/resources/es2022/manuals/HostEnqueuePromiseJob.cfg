def HostEnqueuePromiseJob(job: unknown, realm: unknown) {
  0: call %0 = clo<GetActiveScriptOrModule>() -> 1
  1: {
    let newJob = (new PendingJob("Job" -> job, "Realm" -> realm, "ScriptOrModule" -> %0))[#0]
    push @JOB_QUEUE < newJob
    return undefined
  }
}
