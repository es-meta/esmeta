0: @main f(x: any) {
  1: {
    let x = 1.2f

    // numbers
    assert (= (- x) -1.2f)
    assert (= (~ x) -2f)

    // math values
    x = 3
    assert (= (- x) -3)
    assert (= (~ x) -4)

    // big integers
    x = 10n
    assert (= (- x) -10n)
    assert (= (~ x) -11n)

    // booleans
    assert (= (! true) false)
    assert (= (! false) true)
  }
}
