def f(): Normal[Math] {
  return 42
}

@main def main() {
  // define reference
  let i = 0
  let str = "abc"
  let list = (new [2, 1]) [#1]
  call comp = clo<f>()
  assert (= i 0)
  assert (= list[0] 2)
  assert (= list[1] 1)
  assert (= str[0] 97cu)
  assert (= str[1] 98cu)
  assert (= str[2] 99cu)
  assert (= comp.Type ~normal~)
  assert (= comp.Value 42)
  assert (= comp.Target ~empty~)
}
