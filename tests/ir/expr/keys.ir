@main def main() {
  let m = (new A(1 -> true, "x" -> null))[#1]
  let keys = (keys m)[#1]
  assert (= keys.length 2)
  assert (contains keys 1)
  assert (contains keys "x")

  // TODO implementation is missing
  //let intkeys = (keys-int m)[#1]
  //assert (= intkeys.length 1)
  //assert (contains intkeys 1)
  //assert (!(contains intkeys "x"))
}
