let obj = (new Record("x" -> 1, "y" -> 2))
let key1 = (map-keys obj)
obj.z = 3
let key2 = (map-keys obj)
assert (= key1.length 2)
assert (= key2.length 3)
assert (contains key1 "x")
assert (contains key1 "y")
assert (contains key2 "x")
assert (contains key2 "y")
assert (contains key2 "z")
